import React, { useRef } from 'react';
import CubeModel from './models/Cube';

const Cube: React.FunctionComponent = () => {
    const cube = useRef(new CubeModel());

    return null;
};

export default Cube;